/**
 * Generates a placeholder image URL.
 *
 * These are neutral, license-free stock placeholders (Lorem Picsum) used only
 * as stand-ins for real jewellery photography, which will be supplied by the
 * brand later. Grayscale keeps every placeholder tonally consistent with the
 * design system until real assets are dropped in.
 */
export function placeholderImage(seed: string, width: number, height: number) {
  return "/images/jweleryimage.jpg";
}
